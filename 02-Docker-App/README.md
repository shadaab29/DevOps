# Dockerized Web App (Static Site)

This project demonstrates how to build and run a simple static HTML web page inside a Docker container using Nginx.

## 🔧 Tools Used
- Docker
- Docker Compose
- Nginx

## 📝 Project Structure

